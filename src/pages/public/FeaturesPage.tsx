import PublicPlaceholderPage from "@/components/PublicPlaceholderPage";
import { Layers } from "lucide-react";
export default function FeaturesPage() {
  return <PublicPlaceholderPage title="Funktioner" description="Utforska Cogniq:s alla funktioner — från automatiserad kontering och AI-assistent till budget, prognos och integrationer med svenska banker och e-handelsplattformar." icon={Layers} />;
}
